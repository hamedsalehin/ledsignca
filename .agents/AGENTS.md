# Project Rules

- **NEVER run `git push` without explicit user permission.**
- Always explain changes locally, test them, and wait for the user to explicitly say "push" or give permission before pushing code to GitHub.
